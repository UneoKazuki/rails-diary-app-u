
<%= render @idea %>

<% if user_signed_in? %>

  <div class="btn-toolbar">
    <%= link_to "編集", edit_idea_path(@idea), class: "btn btn-info" %>
  

    <%= button_to "削除", @idea, method: :delete, class: "btn btn-danger ms-3" %>

    <%= link_to "戻る", ideas_path, class: "btn btn-secondary ms-3" %>
  </div>

<% end %>
